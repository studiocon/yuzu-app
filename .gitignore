node_modules
.next
.env*.local
.DS_Store
*.tsbuildinfo

# Claude Code: 共有設定（settings.json / agents / skills）は commit する。
# ローカル個別の許可リストや作業ログのみ無視。
.claude/settings.local.json
.claude/*.local.json
supabase/.temp/

# MCP 設定（project_ref を含む。公開リポジトリには出さない）
.mcp.json
